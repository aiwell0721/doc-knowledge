# Doc-Knowledge 转换器设计

**创建时间**：2026-05-17
**更新日期**：2026-08-13
**版本**：v0.4.0

---

> **技术选型原则**：优先复用成熟开源方案，不重复造轮子。自研仅用于差异化能力（去重、价值评分、自动标签）。
>
> **2026-05-26 架构简化**：Phase 1 自研的 `BaseConverter` / `DocxConverter` / `PdfConverter` / `MarkdownInjector` 已被 MarkItDown 完全替代，相关代码和测试已移除。转换层从 ~500 行精简为 150 行纯封装。
>
> **2026-05-26 图片映射修复**：不再硬编码 MarkItDown 内部命名，改为扫描 Markdown 实际引用后按位置匹配，解耦对 MarkItDown 实现细节的依赖。
>
> **2026-05-27 图片引用策略**：`_fixup_image_refs()` 在 B→C 提取时自动将图片引用改为 `../mirror/...` 格式，指回镜像目录 B。C 只保留提炼后的知识文档，原始图片留在 B，避免冗余拷贝。
>
> **2026-06-14 vision/ocr 概念合并**：`convert_file()` 的 `vision_service` 参数被移除，所有图片识别统一通过 `ocr_service`（详见 [08-OCR统一管道设计](./08-OCR统一管道设计.md)）。`LLMVisionService` 降级为 `CloudOCRService` 的内部实现，不再作为公共 API。CLI 的 `--vision/--api-url/--api-key/--model` 选项已移除，请使用 `--ocr cloud --ocr-api-url --ocr-api-key --ocr-model`。
>
> **2026-08-09 PPTX 图片映射修复（v0.3.1）**：`_build_image_map()` 返回值从 `dict[str, str]` 改为 `list[tuple[str, str]]`。根因是 MarkItDown `_pptx_converter.py:151` 对全中文 shape.name 执行 `re.sub(r"\W", "", name)`，中文字符全部被过滤后引用名退化为 `.jpg`，546 张图片引用名全相同，dict 去重后只剩 1 条映射。list 保留重复条目并按位置匹配（第 i 条引用 → 第 i 张提取图片），同时新增 `_filter_meaningless_images()` 在 OCR 前过滤低价值图片。

---

## 1. 转换引擎：MarkItDown

项目使用微软 [MarkItDown](https://github.com/microsoft/markitdown)（MIT 许可，123K+ stars）作为唯一转换引擎，覆盖 14+ 格式。

```python
# converters/__init__.py — 核心入口
def convert_file(filepath: Path, output_dir: Path | None = None,
                 ocr_service=None, verbose: bool = False
) -> tuple[str, int, list[tuple[str, str]]]:
    """使用 MarkItDown 将文件转换为 Markdown，返回 (文本, 图片数, 图片映射列表)"""
    from markitdown import MarkItDown
    md = MarkItDown()
    result = md.convert(str(filepath))
    # ... 图片提取 + OCR 识别（可选，统一接口）
    return markdown, images_extracted, image_map
```

## 2. 图片提取

MarkItDown 将 PPTX/DOCX 中的图片转为内联引用。为支持后续的 OCR 识别，项目独立提取图片并建立映射：

| 格式 | 提取方式 | 图片目录 |
|------|---------|---------|
| PPTX | `python-pptx` 遍历 shape.image | `{filename}_images/` |
| DOCX | `zipfile` 解压 `word/media/` | `{filename}_images/` |

`_build_image_map()` 扫描 MarkItDown 实际输出的图片引用，按出现顺序与提取的图片路径一一匹配，构造映射列表 `[(ref_name, actual_path), ...]`。不依赖 MarkItDown 内部命名规则。

**位置匹配策略（v0.3.1）**：映射用 **list 而非 dict**。MarkItDown 对 PPTX 中全中文 shape.name 执行 `re.sub(r"\W", "", name)` 后引用名退化为 `.jpg`（中文字符全被过滤），大量图片引用名重复；若用 dict，重复引用会被合并成一条，仅剩 1 张图片可映射。list 保留重复条目，第 i 条引用 → 第 i 张提取图片。调用方须用 `markdown.replace(f"]({ref})", f"]({new_path})", 1)` 逐次替换第一个匹配项，保证各引用指向不同图片。

## 3. 图片识别（可选）

通过统一的 `OCRService` 接口（详见 [08-OCR统一管道设计](./08-OCR统一管道设计.md)）对提取的图片进行批量识别，结果以 blockquote 形式注入 Markdown：

```
![图片](path)

> 📷 **图片识别**: 这是一张架构图，展示了...
```

两种识别场景：
- **PPTX/DOCX 嵌入图片**：保留原图引用，追加 blockquote 描述（增强）
- **扫描型 PDF 整页**：替换全部内容为按页拼接的识别文本

底层 `CloudOCRService` 复用 `ImageFilter`（过滤纯色/过小/低分辨率图片）和 `ThreadPoolExecutor`（最多 5 并发）。

**v0.3.1 新增 `_filter_meaningless_images()`**：在 `convert_file` 调用 `ocr_service.recognize_batch()` 之前过滤低价值图片（文件 < 500B、宽或高 < 50px、纯色），复用 `vision.ImageFilter` 逻辑，减少无效的云端 API / 本地识别调用。过滤只影响"是否追加识别描述"，不影响图片提取与引用映射。

**v0.3.1 blockquote 按位置注入（`_inject_ocr_blockquotes`）**：OCR 结果以 blockquote 追加到**各自图片引用下方**。实现用一次 `re.sub` 从左到右扫描全部本地图片引用（第 i 条引用 ↔ `image_map[i]`），插入的文本不会被重扫——避免 `str.replace(..., 1)` 因替换文本自身含 `.jpg` 锚点而反复匹配同一位置，导致全部识别结果堆叠在第一张图片处。`.jpg` 引用保留，路径替换仍由调用方统一完成。

## 4. 支持的文件类型

```python
def get_supported_extensions() -> list[str]:
    return [
        ".pdf", ".docx", ".pptx", ".xlsx", ".xls",
        ".html", ".htm", ".epub", ".csv",
        ".txt", ".md",
        ".png", ".jpg", ".jpeg", ".gif", ".bmp", ".webp",
        ".mp3", ".wav", ".m4a",
        ".zip", ".msg", ".ipynb",
    ]
```

### 文件分类与处理策略

| 文件类型 | 示例 | 处理策略 |
|---------|------|---------|
| 可转换格式 | `.pdf`, `.docx`, `.pptx`, `.xlsx` | MarkItDown 转为 `.md` |
| 图片 | `.png`, `.jpg`, `.gif` | 直接复制到输出目录 |
| 其他 | `.mp4`, `.zip`, `.exe` | 复制 + 轻量 `.md` 元数据包装 |

## 5. 输出目录结构（目录 B）

```
目录 B/
├── project/
│   ├── 报告.pptx.md                ← MarkItDown 转换产物
│   ├── 报告.pptx_images/           ← 提取的图片
│   │   ├── slide1_img1.png
│   │   └── slide2_img1.jpg
│   ├── 视频.mp4                    ← 不支持格式：原文件复制
│   ├── 视频.mp4.md                 ← 不支持格式：元数据包装
│   └── 照片.jpg                    ← 图片：直接复制
```

每个 Markdown 文件带有 YAML frontmatter，记录来源路径、原始格式、转换状态等元数据。

## 6. PPTX 版式/结构标注（--with-layout，v0.4.0）

**动机**：非 OCR 模式解析 PPTX 只输出 MarkItDown 内容流（标题/列表/表格），页面的空间关系（标题、正文、表格、图片的相对布局）被拍扁。`--with-layout` 为每页注入一行版式标注，辅助理解胶片的结构与图文关系。基于 python-pptx 的结构化信息，**不渲染、不 OCR、不解包 XML**。

**接口变更**：

```python
def convert_file(filepath, output_dir=None, ocr_service=None, verbose=False,
                 with_layout: bool = False) -> tuple[str, int, list[tuple[str, str]]]:
```

- `convert_file` 新增可选参数 `with_layout`（默认 `False`，不改变现有输出）
- CLI `convert --with-layout` → `_run_convert(with_layout=...)` → `convert_file`

**实现**：

1. `_extract_pptx_layout(filepath)` 遍历 `prs.slides`，对每页生成 shape 角色列表，按元素顺序记录：
   - 标题占位符（`shape.is_placeholder` + `placeholder_format.type` 为标题类）
   - 正文占位符 / 文本框（`has_text_frame`）
   - 表格（`has_table`，行×列）
   - 图片（`shape_type == 13` Picture）
   - 图表（`has_chart`）
   - 分组（`shape_type == 6` Group）
   - **相对区域**：基于 `shape.left/top/width/height` 与 slide 宽高比例，归纳为"整页宽/左上/右上/左下/右下/左栏/右栏"等粗粒度方位（不含精确像素坐标，对语义理解无增益、避免输出噪音）

2. `_inject_layout_blocks(markdown, layouts)` 按 `<!-- Slide number: N -->` 锚点正则切分 MarkItDown 输出，在第 N 页锚点行之后、正文之前插入 `> 🗂 **版式**: ...` 引用块，再重组。

**页码对齐**：`prs.slides[N-1]` ↔ `<!-- Slide number: N -->`。MarkItDown 对 PPTX 输出按文件顺序为每页生成锚点注释（实测样例 10 页 → 10 锚点），与 python-pptx 遍历顺序一致。某页无锚点（MarkItDown 未输出，如空页）时跳过该页标注，不崩溃。

**输出示例**：

```
<!-- Slide number: 3 -->
> 🗂 **版式**: 标题(整页宽) · 正文(左上) · 数据表 3×4(右下)

# 市场分析
...
```

**边界与决策**：

- **不含精确坐标**：a:off/a:ext 像素值对"理解内容"无增益，仅版式复刻/UI 还原等生产力场景才需要，故不做 XML 解包
- **默认关闭**：`with_layout=False` 保持现有输出与测试不变
- **不作用于**：非 PPTX 文件；slide 模式（整页 VLM 由模型像素级理解版式，无需标注）
- **可选依赖容错**：python-pptx 未安装时静默返回空标注，不抛错（与 `_extract_pptx_images` 一致）
